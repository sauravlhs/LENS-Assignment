name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run Tests
        run: npm test

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      #- name: Push to Docker Hub
      #  env:
       #   DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        #  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        env: 
          DEBUG: true
        run: docker build -f Dockerfile -t lensassignment:latest . || echo "Docker build failed"


      - name: Push Docker Image
        env: 
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      # run: docker push lensassignment:latest || echo "Docker push failed"

    #  - name: Push to Google Container Registry
    #    env:
     #     GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      #    GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
       # run: |
      #    echo "$GCP_SA_KEY" | base64 --decode > "${HOME}/gcloud.json"
     #     gcloud auth activate-service-account --key-file="${HOME}/gcloud.json"
       #   gcloud --quiet auth configure-docker
        #  docker tag lensassignment:latest gcr.io/${GCP_PROJECT_ID}/lensassignment:latest
         # docker push gcr.io/${GCP_PROJECT_ID}/lensassignment:latest

 # deploy:
  #  needs: build
   # runs-on: ubuntu-latest
    #steps:
     # - name: Deploy to GCP Cloud Run
      #  run: ./deploy.sh  # Add deploy script to deploy to Cloud Run
